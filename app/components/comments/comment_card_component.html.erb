<div data-controller="comments--comment-card-component">
    <%= @comment.body %>
    <div class=" flex text-gray-500/70 italic gap-x-1 mb-4">
            <p>Last comment 3days ago</p>
            <%= link_to ". Edit .", edit_group_post_path(@post) %>
            <%=form_with model: Post.new, url: group_post_path(@post), method: :delete do %>
               <button class="text-red-300">Delete .</button>
            <% end %> 
            <p>Created by <%= @post.user == current_user ? "You" : @post.user.name %></p>
        </div>
    <% @comment.replies.each do |reply| %>
            <div class=" ml-16">
                <h3 class="text-blue-500"><%= reply.body %></h3>  
                <div class=" flex text-gray-500/70 italic gap-x-1 mb-4">
                    <p>Last comment 3days ago</p>
                    <%= link_to ". Edit .", edit_group_post_path(@post) %>
                    <%=form_with model: Post.new, url: group_post_path(@post), method: :delete do %>
                    <button class="text-red-300">Delete .</button>
                    <% end %> 
                    <p>Created by <%= @post.user == current_user ? "You" : @post.user.name %></p>
                </div> 
            </div>
    <% end %>

    <%# <%= form_with model: Reply.new, url: group_post_comment_replies_path(@post, @comment), method: :post do |f| %>
        <%# <%= f.text_field :body, class: "w-full border-2 border-gray-300 p-4 rounded-lg focus:outline-none focus:border-blue-500" %>
        <%# <%= f.submit "Reply", class: "bg-blue-500 text-white px-4 py-2 rounded font-medium mt-4" %>
    <%# <% end %>  
</div>
